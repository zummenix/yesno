[package]
name = "yesno"
version = "0.1.0"
authors = ["Aleksey Kuznetsov <zummenix@gmail.com>"]
edition = "2018"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
minreq = { version = "2.1", features = ["json-using-serde", "https"] }

[profile.release]
panic = "abort"
lto = true

